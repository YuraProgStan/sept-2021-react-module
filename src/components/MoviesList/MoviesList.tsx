import React, {FC} from 'react';
import {Outlet} from "react-router-dom";

const MoviesList:FC = () => {
    return (

        <div>
            MoviesList
        </div>


    );
};

export default MoviesList;